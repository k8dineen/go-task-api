# go-task-api
Simple task manager creates a REST API using Go (gin framework).
Clients can send HTTP requests to interact with tasks (GET, POST, etc.).
The server processes the request and returns a JSON response.

